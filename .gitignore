# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Node / Vite
node_modules/
dist/

# Env and OS
.env
.env.local
.DS_Store

# Parallel agent worktrees (scripts/agents.sh). The venv is a symlink there,
# which backend/.gitignore's ".venv/" does not match.
.agent-slot
backend/.venv

# Claude Code agent worktrees
.claude/worktrees/
